const languages = ['html', 'css', 'javascript', 'react', 'node', 'postgres'];
console.log('These are the languages: ', languages);

const firstElement = languages[0];
console.log('This is the first element in languages: ', firstElement);

const thirdElement = languages[2];
console.log('This is the third element in languages: ', thirdElement);

const length = languages.length;
console.log('The length of the languages array is: ', length);

const lastElement = languages[languages.length - 1];
console.log('This is the last element in languages: ', lastElement);
